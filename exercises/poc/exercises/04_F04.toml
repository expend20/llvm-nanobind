title = "Typed Integer Globals"
track = "foundations"
level = "beginner"
estimated_minutes = 10
submission_mode = "function_or_script"
allow_stdout_ir = true
prompt = """
Add two globals initialized to 7:
  @g_i32 = global i32 7
  @g_i64 = global i64 7
Return valid IR.
"""
hints = [
  "`add_global` takes `(type, name)`.",
  "Use the matching integer width for each initializer.",
]
