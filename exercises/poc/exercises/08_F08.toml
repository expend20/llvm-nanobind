title = "Internal Global Counter"
track = "foundations"
level = "beginner"
estimated_minutes = 10
submission_mode = "function_or_script"
allow_stdout_ir = true
prompt = """
Add an internal global:
  @counter = internal global i32 0
Return valid IR.
"""
hints = [
  "Set linkage with `llvm.Linkage.Internal`.",
  "Global initializer type must match the global type.",
]
